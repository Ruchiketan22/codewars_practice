def wave(a):
    result=[]
    
    for i in range(len(a)):
        if a[i] == ' ':
            continue
        wave_words= a[:i] + a[i].upper() + a[i+1:]
        result.append(wave_words)

    return result
